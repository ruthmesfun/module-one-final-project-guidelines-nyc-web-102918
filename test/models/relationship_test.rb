require 'test_helper'

class RelationshipTest < ActiveSupport::TestCase

    #if we have time we can validate the information we get

    # def setup
    #     @relationship = Relationship.new(recommender_id: users(:ruth).id, followed_id: users(:ninja).if)
    # end

end